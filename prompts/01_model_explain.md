# 第一步：模型解释

目的：

- 解释模型为什么选中这只股票。

只使用：

- `model_score`
- `feature_contrib`
- `factor_definitions`（全局因子释义表，辅助理解各因子含义）

应该做：

- 总结驱动入选的主要特征。
- 区分强驱动与弱驱动。
- 说明模型逻辑中不完整或模糊的部分。

不要做：

- 不看财务报表
- 不看新闻或公告
- 不展开行业周期讨论
- 不给最终评级
